let initialState = {
    friends:[
        {id: 1, name: 'Ivan'},
        {id: 2, name: 'Oleg'},
        {id: 3, name: 'Dmitry'},
    ]
};

const sidebarReducer = (state = initialState, action) => {

    return state;
}

export default sidebarReducer;